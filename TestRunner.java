package webAutomation;

// TestRunner class for executing the GoogleSearch test
public class TestRunner {
    
    // Main method to run the GoogleSearch test
    public static void main(String[] args) {
        
        // Create an instance of the GoogleSearch class
        GoogleSearch googleSearchTest = new GoogleSearch();
        
        // Call the googleSearch method to perform the test
        googleSearchTest.googleSearch();
    }
}
